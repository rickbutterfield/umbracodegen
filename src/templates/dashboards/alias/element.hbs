import { UmbElementMixin } from "@umbraco-cms/backoffice/element-api";
import { LitElement, html, css, customElement } from "@umbraco-cms/backoffice/external/lit";

/**
* @element {{ elementName name }}DashboardElement
*/
@customElement('{{tagName name}}-dashboard')
export class {{ elementName name }}DashboardElement extends UmbElementMixin(LitElement) {
  constructor() {
    super();
  }

  render() {
    return html`
      <uui-box headline="This is your new {{ elementName name }} dashboard generated by umbracodegen">
      </uui-box>
    `;
  }

  static styles = 
    css`
      :host {
        display: block;
        padding: 20px;
      }
    `;
}

export default {{ elementName name }}DashboardElement;