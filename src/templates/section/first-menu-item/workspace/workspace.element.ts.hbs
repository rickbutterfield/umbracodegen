{{#contains "context" components this}}
import { {{constantCase alias}}_CONTEXT, {{pascalCase alias}}Context } from "../../../context/{{kebabCase alias}}.context";
{{/contains}}
import { UmbLitElement } from "@umbraco-cms/backoffice/lit-element";
import { customElement, html } from "@umbraco-cms/backoffice/external/lit";

const elementName = "{{kebabCase alias}}-workspace";

@customElement(elementName)
export class {{pascalCase alias}}WorkspaceElement extends UmbLitElement {
  {{#contains "context" components this}}
  #{{camelCase alias}}Context?: {{pascalCase alias}}Context;
  {{/contains}}

  constructor() {
    super();
  {{#contains "context" components this}}
    this.consumeContext({{constantCase alias}}_CONTEXT, (instance) => {
      this.#{{camelCase alias}}Context = instance;
    });

    const context = this.#{{camelCase alias}}Context;
  {{/contains}}
  }

  render() {
    return html`
      <umb-body-layout headline="Overview">
        <div id="main">
          <uui-box headline="Overview">
            This is a menu item generated by <pre>umbracodegen</pre>. Go to <pre>workspace.element.ts</pre> to edit it.
          </uui-box>
        </div>
      </umb-body-layout>
    `;
  }
}

export default {{pascalCase alias}}WorkspaceElement;

declare global {
  interface HTMLElementTagNameMap {
    [elementName]: {{pascalCase alias}}WorkspaceElement;
  }
}
